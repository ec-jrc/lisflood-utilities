numpy==1.14.0
PyYAML==3.12
netCDF4==1.3.1
# You have to install the same GDAL python package version of gdal library
# e.g.: if you installed gdal-2.1.0 you have to install GDAL in the following way:
# pip install GDAL==2.1.0
GDAL
