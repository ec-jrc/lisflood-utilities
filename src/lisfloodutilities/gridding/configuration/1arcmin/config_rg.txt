[PROPERTIES]

VAR_CODE = rg
CELL_METHODS = time: sum
UNIT = J/m2
UNIT_CONVERSION = 1.0
VALUE_MIN = 0
VALUE_MAX = 115000000
VALUE_SCALE = 10000.0
VALUE_OFFSET = 0.0
DATA_TYPE_PACKED = i2
STANDARD_NAME = integral_wrt_time_of_surface_downwelling_shortwave_flux_in_air
LONG_NAME = Daily Calculated Radiation

KIWIS_FILTER_PLUGIN_CLASSES = {'SolarRadiationLimitsKiwisFilter': {'EXCLUDE_BELLOW_LATITUDE': 66.0, 'EXCLUDE_BELLOW_VALUE': 0.0}}

[KISTERS_API]

# Identification of the timeseries inside the API. Identifies the variable to be downloaded.
API_TIMESERIES_ID: 236118

# Setup of the Hour to download the metadata from.
# Each variable have a specific hour for the data availability
API_TIMESERIES_TIME_TO_DOWNLOAD: 00

# Identifies which data should be downloaded from the timeseries. Total, Average, Min, Max, ...
# Example: "*/*/Precip/6h.Total" to get the total precipitation
API_TIMESERIES_PATH_WILDCARD: */*/SunRad/Day.Energy

[VAR_TIME]

UNIT = days since 1990-01-02 00:00:00.0
FREQUENCY = 1

# Used to force the time value when it is not defined in the filename. Format: HHMM
# FORCE_TIME = 0000

# Used to allow moving the files 1 day forward for variables (WS, RG, PD, PR?)
OFFSET_FILE_DATE = 1

